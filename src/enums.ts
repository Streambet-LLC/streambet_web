export enum CurrencyType {
  FREE_TOKENS = 'free_tokens',
  STREAM_COINS = 'stream_coins',
}

export enum StreamPlatform {
  Kick = 'kick',
  Youtube = 'youtube',
  Twitch = 'twitch',
  Vimeo = 'vimeo',
}
